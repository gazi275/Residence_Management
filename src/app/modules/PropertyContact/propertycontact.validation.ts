
import { z } from "zod";

const createpropertyContactSchema = z.object({
	
});
const updatepropertyContactSchema = z.object({
	
});

export const PropertyContactValidations = {
	createpropertyContactSchema,
    updatepropertyContactSchema,
};

